#include <string>
#include <map>
#include <iostream>

int main () {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    std::cout.tie(0);

    std::string s;
    std::cin >> s;

    std::map<std::string, int> count;

    int i = 0;
    int start = 0;
    while (i < s.length()) {
        if (s[i] - '0' <= 9) {}
    }
}